import 'package:flutter/material.dart';
import 'login.dart'; // Assuming login.dart is in the same directory

void main() {
  runApp(MaterialApp(
    home: Myapp(), // Assuming your login screen is named Myapp in login.dart
  ));
}
